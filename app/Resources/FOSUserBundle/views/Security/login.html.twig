{% extends "base.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block header %} {% endblock %}
{% block body %}
<body class="error-body no-top lazy"  data-original="{{ asset('frontend/assets/img/work.jpg') }}"  style="background-image: url('{{ asset('frontend/assets/img/work.jpg') }}')">

<div class="container">
    <div class="row login-container animated fadeInUp">
        <div class="col-md-7 col-md-offset-2 tiles white no-padding">
            <div class="p-t-30 p-l-40 p-b-20 xs-p-t-10 xs-p-l-10 xs-p-b-10">
                <h2 class="center-text">Sign in to <img src="{{ asset('frontend/img/logo.png') }}" alt="" height="50"></h2>
                <p>Use your username and password to Sign in<br></p>
            </div>
            <div class="tiles grey p-t-20 p-b-20 no-margin text-black tab-content">
                <div role="tabpanel" class="tab-pane active" id="tab_login">
                    <form class="animated fadeIn validate" action="{{ path("fos_user_security_check") }}" method="post" >
                        {% if error %}
                            <p>{{ error.messageKey|trans(error.messageData, 'security') }}</p>
                        {% endif %}
                        <div class="row form-row m-l-20 m-r-20 xs-m-l-10 xs-m-r-10">
                            <div class="col-md-6 col-sm-6">
                                <input type="text" id="username" name="_username" value="{{ last_username }}" required="required" placeholder="{{ 'security.login.username'|trans }}"/>
                            </div>
                            <div class="col-md-6 col-sm-6">
                                <input type="password" id="password" name="_password" required="required" placeholder="{{ 'security.login.password'|trans }}"/>
                            </div>
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                        </div>
                        <div class="row p-t-10 m-l-20 m-r-20 xs-m-l-10 xs-m-r-10">
                            <div class="control-group col-md-10">
                                <div class="checkbox checkbox check-success">
                                    <a href="{{ path('fos_user_resetting_request') }}">Forgot password ?</a>
                                    &nbsp;&nbsp;
                                    <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                                    <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>
                                </div>
                            </div>
                        </div>
                        <div class="row p-t-10 m-l-20 m-r-20 xs-m-l-10 xs-m-r-10">
                            <div class="control-group col-md-10">
                                <input type="submit" class="btn btn-success" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

{% include 'javascripts.html.twig' %}

</body>
{% endblock %}
